# Project Registration Workflow

**문서 버전:** 1.0
**작성일:** 2025-12-31
**대상:** 빌더 계정 사용자의 프로젝트 등록 프로세스

---

## 목차

1. [개요](#1-개요)
2. [프로젝트 등록 프로세스](#2-프로젝트-등록-프로세스)
3. [데이터베이스 상태 관리](#3-데이터베이스-상태-관리)
4. [안내문 분기 처리](#4-안내문-분기-처리)
5. [다운로드 완료 처리](#5-다운로드-완료-처리)
6. [관련 파일 목록](#6-관련-파일-목록)

---

## 1. 개요

### 1.1 목적

빌더 계정 사용자가 새 프로젝트를 등록할 때, 필요한 개발환경 설정을 안내하고 Dev Package 다운로드를 통해 프로젝트 등록을 완료하는 프로세스를 정의합니다.

### 1.2 핵심 원칙

- **첫 번째 프로젝트**: 전체 개발환경 설정 안내 (Dev Package + Git + Node.js + Claude Code)
- **두 번째 이후 프로젝트**: 간단한 새 폴더 준비 안내
- **등록 완료 조건**: Dev Package 다운로드 버튼 클릭

### 1.3 등록 완료 기준

| 구분 | 등록 완료 기준 | 이유 |
|------|---------------|------|
| Dev Package 다운로드 | 다운로드 버튼 클릭 시 완료 | 브라우저 제약으로 실제 다운로드 완료 여부 추적 불가 |

---

## 2. 프로젝트 등록 프로세스

### 2.1 전체 흐름도

```
┌────────────────────────────────────────────────────────────────────┐
│                    프로젝트 등록 프로세스                            │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  [1] 사용자가 프로젝트 정보 입력                                     │
│       ↓                                                            │
│  [2] DB에 프로젝트 저장                                             │
│       registration_complete = false                                │
│       ↓                                                            │
│  [3] 프로젝트 수에 따른 안내문 분기                                   │
│       ├─ 첫 번째 프로젝트 → 전체 개발환경 설정 안내                   │
│       └─ 두 번째 이후 → 새 프로젝트 폴더 준비 안내                    │
│       ↓                                                            │
│  [4] 안내문에서 다운로드 페이지 링크 클릭                             │
│       ↓                                                            │
│  [5] My Page > 파일 다운로드 페이지 이동                             │
│       ↓                                                            │
│  [6] Dev Package 다운로드 버튼 클릭                                  │
│       ↓                                                            │
│  [7] 다운로드 시작 + DB 업데이트                                     │
│       registration_complete = true                                 │
│       ↓                                                            │
│  [8] 프로젝트 등록 완료                                              │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

### 2.2 단계별 상세

#### Step 1: 프로젝트 정보 입력

**위치:** 대시보드 메인 (`index.html`)

**입력 항목:**
| 필드 | 필수 | 설명 |
|------|:----:|------|
| project_name | ✅ | 프로젝트 이름 |
| project_description | ❌ | 프로젝트 설명 |

#### Step 2: DB에 프로젝트 저장

**테이블:** `projects`

**저장 데이터:**
```javascript
{
    user_id: currentUser.user_id,
    project_name: projectName,
    project_description: description,
    registration_complete: false,  // ← 핵심: 등록 미완료 상태
    created_at: new Date().toISOString()
}
```

#### Step 3: 안내문 분기 처리

**분기 조건:**
```javascript
const existingProjects = await getExistingProjects(userId);

if (existingProjects.length === 0) {
    // 첫 번째 프로젝트
    showPackageInstallGuide();
} else {
    // 두 번째 이후 프로젝트
    showNewProjectFolderGuide();
}
```

#### Step 4-5: 다운로드 페이지 이동

**안내문 내 링크:**
```markdown
👉 **[파일 다운로드 페이지로 이동](/pages/mypage/downloads.html)**
```

#### Step 6-7: Dev Package 다운로드

**다운로드 버튼 클릭 시 처리:**
```javascript
async function downloadPackage() {
    // 1. 다운로드 실행
    downloadFile('Dev_Package.zip');

    // 2. registration_complete 업데이트
    await supabaseClient
        .from('projects')
        .update({ registration_complete: true })
        .eq('user_id', userData.user_id)
        .eq('registration_complete', false);
}
```

---

## 3. 데이터베이스 상태 관리

### 3.1 projects 테이블 스키마 (등록 관련 필드)

| 컬럼명 | 타입 | 기본값 | 설명 |
|--------|------|--------|------|
| project_id | UUID | auto | 프로젝트 고유 ID |
| user_id | VARCHAR(8) | - | 사용자 ID |
| project_name | VARCHAR(255) | - | 프로젝트 이름 |
| registration_complete | BOOLEAN | false | 등록 완료 여부 |
| created_at | TIMESTAMP | NOW() | 생성 시각 |

### 3.2 상태 전이

```
┌─────────────────┐                    ┌─────────────────┐
│  프로젝트 생성   │                    │  등록 완료       │
│ registration_   │ ─────────────────→ │ registration_   │
│ complete=false  │   다운로드 클릭      │ complete=true   │
└─────────────────┘                    └─────────────────┘
```

### 3.3 SQL 예시

**프로젝트 생성:**
```sql
INSERT INTO projects (user_id, project_name, registration_complete)
VALUES ('U1234567', '새 프로젝트', false);
```

**등록 완료 처리:**
```sql
UPDATE projects
SET registration_complete = true
WHERE user_id = 'U1234567'
  AND registration_complete = false;
```

**등록 미완료 프로젝트 조회:**
```sql
SELECT * FROM projects
WHERE user_id = 'U1234567'
  AND registration_complete = false;
```

---

## 4. 안내문 분기 처리

### 4.1 안내문 파일 목록

| 대상 | 파일명 | 위치 |
|------|--------|------|
| 첫 번째 프로젝트 | Project_Registration.md | Briefings_OrderSheets/Briefings/Situational/ |
| 두 번째 이후 | Project_Registration_Subsequent.md | Briefings_OrderSheets/Briefings/Situational/ |

### 4.2 첫 번째 프로젝트 안내문 주요 내용

**제목:** 처음 프로젝트 개발환경설정 가이드

**포함 내용:**
1. Dev Package 설명 및 구성
2. Git 설치 안내
3. Node.js 설치 안내
4. Claude Code 설치 안내
5. 다운로드 페이지 링크

### 4.3 두 번째 이후 프로젝트 안내문 주요 내용

**제목:** 새 프로젝트 폴더 준비 안내

**포함 내용:**
1. 새 프로젝트용 Dev Package 다운로드 안내
2. 폴더 구조 설명
3. 다운로드 페이지 링크

### 4.4 분기 로직 (index.html)

```javascript
// loadUserProject 함수 내

// 사용자의 기존 프로젝트 수 확인
const { data: existingProjects } = await supabaseClient
    .from('projects')
    .select('project_id')
    .eq('user_id', userData.user_id);

const isFirstProject = !existingProjects || existingProjects.length === 0;

// 새 프로젝트 저장 후 분기
if (isFirstProject) {
    // 첫 번째 프로젝트: 전체 개발환경 설정 안내
    showPackageInstallGuide();
} else {
    // 두 번째 이후: 새 폴더 준비 안내
    showNewProjectFolderGuide();
}
```

---

## 5. 다운로드 완료 처리

### 5.1 처리 위치

| 파일 | 기능 | 설명 |
|------|------|------|
| index.html | downloadPackage() | 대시보드에서 다운로드 |
| pages/mypage/downloads.html | downloadDevPackage() | 마이페이지에서 다운로드 |

### 5.2 다운로드 처리 코드

**downloads.html:**
```javascript
async function downloadDevPackage() {
    // 1. 실제 다운로드 실행
    const link = document.createElement('a');
    link.href = '/assets/downloads/Dev_Package.zip';
    link.download = 'Dev_Package.zip';
    link.click();

    // 2. 프로젝트 등록 완료 처리
    try {
        await supabaseClient
            .from('projects')
            .update({ registration_complete: true })
            .eq('user_id', userData.user_id)
            .eq('registration_complete', false);

        console.log('✅ 프로젝트 등록 완료 처리됨');
    } catch (error) {
        console.log('등록 완료 처리 실패 (무시):', error);
    }
}
```

### 5.3 왜 다운로드 클릭 = 등록 완료인가?

**브라우저 제약:**
- JavaScript로 실제 파일 다운로드 완료 여부를 추적할 수 없음
- 사용자가 "저장" 또는 "취소"를 선택했는지 알 수 없음

**합리적 가정:**
- 다운로드 버튼을 클릭했다면, 대부분 실제로 다운로드 의도가 있음
- 실패하더라도 사용자가 재시도할 수 있음
- 따라서 **클릭 = 완료**로 간주하는 것이 가장 실용적

---

## 6. 관련 파일 목록

### 6.1 프론트엔드

| 파일 | 경로 | 역할 |
|------|------|------|
| index.html | Production/ | 메인 대시보드, 프로젝트 생성/분기 처리 |
| downloads.html | Production/pages/mypage/ | Dev Package 다운로드 페이지 |

### 6.2 안내문

| 파일 | 경로 | 역할 |
|------|------|------|
| Project_Registration.md | Briefings_OrderSheets/Briefings/Situational/ | 첫 번째 프로젝트 안내 |
| Project_Registration_Subsequent.md | Briefings_OrderSheets/Briefings/Situational/ | 두 번째 이후 프로젝트 안내 |
| guides.js | Briefings_OrderSheets/Briefings/ | 안내문 번들 파일 |

### 6.3 데이터베이스

| 테이블 | 역할 |
|--------|------|
| projects | 프로젝트 정보 및 등록 상태 관리 |

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| 1.0 | 2025-12-31 | 최초 작성 - 프로젝트 등록 프로세스 정의 |

---

**문서 버전:** 1.0
**작성일:** 2025-12-31
**작성자:** Claude Code
