# 프로젝트 그리드 DB 참고 자료

## 📋 개요

**PoliticianFinder 실전 프로젝트**에서 실제로 사용 중인 프로젝트 그리드 Supabase DB 관련 파일들입니다.

**SSALWorks 프로젝트에서 프로젝트 그리드 DB를 구축할 때 이 파일들을 참고하세요!**

## 📂 파일 목록

### 1. `project_grid_schema.sql`
**프로젝트 그리드 테이블 스키마**
- PoliticianFinder에서 실제 사용 중인 테이블 구조
- 63개 Task 관리용
- Phase, Area, Task ID 기반 구조

**주요 필드:**
- `phase`: 단계 (1-5)
- `area`: 영역 (O, D, BI, BA, F, T)
- `task_id`: 작업 ID (P1F1, P3BA2 등)
- `task_name`: 작업명
- `progress`: 진행률 (0-100)
- `status`: 상태 (대기, 진행중, 완료)
- `assigned_agent`: 담당 에이전트
- `generated_files`: 생성된 파일 목록
- `test_history`: 테스트 이력
- `build_result`: 빌드 결과

### 2. `project_grid_viewer.html`
**프로젝트 그리드 3D 뷰어**
- Supabase에서 데이터를 가져와 3D로 시각화
- 실시간 업데이트
- Phase/Area별 필터링
- Task 상세 정보 팝업

**사용 방법:**
```html
<!-- Supabase 연결 정보 수정 -->
const SUPABASE_URL = 'your-project-url'
const SUPABASE_ANON_KEY = 'your-anon-key'
```

### 3. `update_project_grid.py`
**프로젝트 그리드 업데이트 Python 스크립트**
- Task 상태 업데이트
- 진행률 업데이트
- Supabase Python 클라이언트 사용

**사용 예:**
```python
python update_project_grid.py --task-id P1F1 --status completed --progress 100
```

### 4. `check_project_grid_schema.py`
**프로젝트 그리드 스키마 확인 스크립트**
- 테이블 구조 확인
- 데이터 무결성 체크

## 🚀 SSALWorks에 적용하는 방법

### Step 1: 스키마 생성
```bash
# project_grid_schema.sql을 Supabase에서 실행
# 또는 3_개발/3-6_Database/Supabase/migrations/에 복사
```

### Step 2: 뷰어 설정
```bash
# project_grid_viewer.html을 프로젝트에 통합
# Supabase 연결 정보 수정
```

### Step 3: Python 스크립트 사용
```bash
# 필요시 update_project_grid.py 수정하여 사용
pip install supabase
python update_project_grid.py
```

## 📌 중요 사항

**이 파일들은 PoliticianFinder 실전 프로젝트에서 검증된 구조입니다:**
- ✅ 실제 프로덕션 환경에서 사용 중
- ✅ 63개 Task 관리 검증 완료
- ✅ 3D 뷰어로 시각화 완료
- ✅ Python 스크립트로 자동화 완료

## 🔗 관련 문서

- 프로젝트 그리드 메인: `2_개발준비/2-4_Project_Grid/`
- 데이터베이스: `3_개발/3-6_Database/`
- 프로젝트 그리드 매뉴얼: `2_개발준비/2-4_Project_Grid/manual/`

## ⚠️ 주의

- 이 파일들은 **참고용**입니다
- SSALWorks 프로젝트에 맞게 **수정**이 필요할 수 있습니다
- Supabase 연결 정보는 `.env`로 관리하세요

---

**출처:** PoliticianFinder 실전 프로젝트
**버전:** REVISED Edition (2025-11-06)
